# NIA ground-truth tools (evaluation only, stage 5)

To be ported from `SL_MST/papers/iccas2026/code/`:

- `build_nia_word_npy.py` — NIA JSON -> GT tensor
- `crop_active_signing_windows.py` — active motion window cropping

Not required for `pafpose run`.
